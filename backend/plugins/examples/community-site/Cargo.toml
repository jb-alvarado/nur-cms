[package]
name = "nur-cms-community-site-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
regex = { version = "1", default-features = false, features = ["std"] }
serde_json = "1"
wit-bindgen = "0.60"

[workspace]
