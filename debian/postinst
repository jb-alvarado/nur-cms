#DEBHELPER#

sysUser="nur-cms"

if [ ! $(id -u $sysUser 2>/dev/null || echo -1) -ge 0 ]; then
    adduser --system --create-home $sysUser > /dev/null || \
        adduser --system --home "/home/$sysUser" $sysUser

    mkdir -p /var/www/nur-cms/uploads
    chown ${sysUser}: -R /var/www/nur-cms
fi
