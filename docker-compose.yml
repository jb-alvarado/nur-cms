services:
  database_postgres:
    image: postgres:17
    ports:
      - 127.0.0.1:5442:5432
    environment:
      POSTGRES_PASSWORD: nuR1234
      POSTGRES_USER: postgres
      POSTGRES_DB: nur_cms
    volumes:
      - ./postgres-data/:/var/lib/postgresql/data:Z
